{
  "name": "vacant-hommie-tailwind",
  "version": "1.0.0",
  "private": true,
  "description": "Tailwind CSS theme app for the vacant_hommie Django project.",
  "scripts": {
    "build": "tailwindcss -i ./src/styles.css -o ../static/css/dist/styles.css --minify",
    "dev": "tailwindcss -i ./src/styles.css -o ../static/css/dist/styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
